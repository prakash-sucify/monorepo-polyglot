{
  "name": "boundaries",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/boundaries/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/boundaries",
        "main": "libs/boundaries/src/index.ts",
        "tsConfig": "libs/boundaries/tsconfig.lib.json",
        "assets": [],
        "format": ["cjs", "esm"]
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/boundaries/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/boundaries/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": ["scope:shared", "type:lib"]
}
